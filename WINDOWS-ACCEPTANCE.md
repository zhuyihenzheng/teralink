# Windows 实机验收（尚未执行）

使用有权限的测试服务器和专用测试账户；不要记录真实密码。

- [ ] 自带运行时包完整解压后可启动；lite 包在已装 .NET 10 Windows Desktop Runtime 的电脑可启动，缺少运行时时提示依赖。
- [ ] 按 README 先解除锁定 ZIP 再解压，运行 `TeraLink.exe` 不出现 SmartScreen 提示；未解除锁定时记录提示文案。`TeraLink.exe` 属性→详细信息显示产品名、说明、版本 0.2.0.0 和版权。
- [ ] 取得证书后：`build.ps1 -CertificateThumbprint` 产出的包签名有效且带时间戳，下载后不再出现 SmartScreen 提示。
- [ ] 100%、150%、200% DPI 下主窗口和可滚动的新增/编辑窗口可用，密码和保存按钮均可访问。
- [ ] 旧版 v1 SSH 连接可读取，加密密码保持不变；保存后为 v2，旧版应用拒绝读取。
- [ ] SSH / RDP 类型切换，默认端口、收藏、分组、搜索、编辑空密码保留、删除和重启恢复正常。
- [ ] 第二实例被阻止；损坏 JSON 不会被重置。
- [ ] RDP 无需安装 Tera Term。直连、IPv4、IPv6、非默认端口、域账户、UPN 和特殊字符密码均可用。
- [ ] 选择已有 `.rdp` 自动读取地址/账户；原文件不变，副本保留网关、路由、显示、设备重定向设置，使用新的账户密码。原文件中的域账户正确处理。
- [ ] 原文件路径含空格/日文/中文可用；丢失、设置被修改、重复字段、编码异常时报错，不提交密码。重新选择文件恢复使用。
- [ ] 原文件含签名时，副本不带失效签名；确认组织允许未签名文件，记录系统原生提示。
- [ ] 同一目标的两个账户使用各自副本；系统已有 TERMSRV 凭据不被覆盖，实际使用正确账户。
- [ ] RDP 密码错误、NLA/策略要求再次输入时，保留系统提示，不显示“登录成功”，不修改组织策略。
- [ ] 记录同一次 RDP 第一层和第二层登录画面类型及账户是否相同（不记录秘密）。第二层自动填写当前不属于已实现功能。
- [ ] Tera Term 5.x 与宏程序路径检测可用，首次指纹确认、拒绝指纹、错误密码、不可达、取消、关闭终端、120 秒超时均正确处理；没有轮询重试密码。
- [ ] Tera Term CheckAuthListFirst 开/关及英文/日文环境，特殊字符密码和 511 UTF-8 字节边界可用。
- [ ] 连接后退出选项保存，RDP 启动后 / SSH 宏完成后工具退出，已有远程会话继续；失败时保留工具显示错误。
- [ ] `.json`、`.rdp` 副本、宏、日志、剪贴板及进程命令行中均无明文测试密码；编辑/删除时移除对应 `.rdp` 副本。
- [ ] Windows 运行检查程序，DPAPI 往返、篡改拒绝、RDP UTF-16LE 编码通过；另一 Windows 账户无法解密。
- [ ] 任务管理器分别记录 TeraLink、Tera Term、ttpmacro、mstsc：启动后空闲 60 秒、连接中、连接后保留窗口、自动退出四种状态的 CPU、工作集、私有内存；自动退出后确认无 TeraLink 进程。比较 lite/完整包时使用相同运行状态。

记录 Windows/Tera Term 版本、DPI、通过项及失败现象。未经这份清单验证，保持 alpha 标记。
