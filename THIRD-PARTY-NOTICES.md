# Third-party notices

The self-contained Windows distribution includes Microsoft .NET runtime and Windows Desktop components. Their MIT licenses and third-party notices are included in `licenses/` and the runtime's own notice files.

Tera Term is a separately installed application and is not redistributed in this package. TeraLink uses its documented macro interface. TeraLink is not affiliated with or endorsed by the Tera Term project.

The application uses no additional NuGet UI, credential-storage or telemetry packages.
